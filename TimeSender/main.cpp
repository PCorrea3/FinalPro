#include <QApplication>
#include "timer.h"
#include <QProcess>

int main(int argc, char *argv[])
{
    QApplication app(argc,argv);
    Timer timer;
    QProcess = new QProcess(this);
    process->start(timer);

    return app.exec();

}
